<nav>
    <ul>
        <li><a href="{{ base_url() }}">Home</a></li>
        {% if authz.isLoggedIn() %}
            <li><a href="{{ path_for('admin') }}">Admin</a></li>
            <li><a href="{{ path_for('log-out') }}">Log out {{ authz.user.email }}</a></li>
        {% else %}
            <li><a href="{{ path_for('log-in') }}">Log in</a></li>
        {% endif %}
    </ul>
</nav>
