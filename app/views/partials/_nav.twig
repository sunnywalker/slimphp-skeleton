<nav>
    <ul>
        <li><a href="{{ base_url() }}">Home</a></li>
        {% if authn.is_logged_in %}
            <li><a href="{{ path_for('admin-home') }}">Admin</a></li>
            <li><a href="{{ path_for('log-out') }}">Log out {{ authn.user.email }}</a></li>
        {% else %}
            <li><a href="{{ path_for('log-in') }}">Log in</a></li>
        {% endif %}
    </ul>
</nav>
